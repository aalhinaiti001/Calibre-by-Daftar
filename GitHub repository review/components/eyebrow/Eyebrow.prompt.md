Wide-tracked uppercase micro-label that opens sections. Use `rule` for the hero's leading hairline, `tone="accent"` when it sits over a section that already uses warm elsewhere.

```jsx
<Eyebrow rule>Hiring Noise Diagnostic</Eyebrow>
<Eyebrow tone="accent">What Leaves the Room</Eyebrow>
```
